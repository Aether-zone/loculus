strict-peer-dependencies=false
auto-install-peers=true
# `pnpm deploy` in pnpm 10 requires this; its --legacy fallback exits non-zero
# even when it writes a correct tree.
inject-workspace-packages=true
